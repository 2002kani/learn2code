import { OpCard } from "./testobj"

const unbenannt = () => {

    const getChar = (num: number) => {
        OpCard[num];
    }

return ( 
    <div>
        <input type="text" />
    </div>
)
}

export default unbenannt